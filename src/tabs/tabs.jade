ul([ngClass]="'slds-tabs--' + type + '__nav'", role="tablist", (keydown.ArrowLeft)="move($event, -1)", (keydown.ArrowRight)="move($event, 1)")
  li.slds-text-heading--label(*ngFor="let tab of tabs", [ngClass]="'slds-tabs--' + type + '__item'", (click)="select(tab)", [class.slds-active]="tab.active", role="presentation")
    a([nglTabHeading]="tab", [ngClass]="'slds-tabs--' + type + '__link'", role="tab")
div([ngClass]="'slds-tabs--' + type + '__content'", role="tabpanel")
  template([ngTemplateOutlet]="activeTab?.templateRef")
